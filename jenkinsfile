pipeline {
    agent { dockerfile true }
    environment {
                HOME = "${env.WORKSPACE}"
                run adduser --gecos "" --disabled-password --quiet jenkins
            }
    stages {
        stage('Test') {
            steps {
                sh 'node --version'
                sh 'svn --version'
            }
        }
    }
}