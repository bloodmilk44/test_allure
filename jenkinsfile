pipeline {
    agent { test.dockerfile true }
    environment {
                HOME = "${env.WORKSPACE}"
            }
    stages {
        stage('Test') {
            steps {
                sh 'node --version'
                sh 'svn --version'
            }
        }
    }
}