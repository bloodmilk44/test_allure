pipeline {
    agent { dockerfile true }
    environment {
                HOME = "${env.WORKSPACE}"
            }
    stages {
        stage('Test') {
            steps {
                sh 'node --version'
                sh 'svn --version'
            }
        }
    }
    steps {
    script {
        wrap([$class: 'Jenkins']) {

            var name = "${Jenkins}"
        }
    }
}

}